RSpec.describe ApplicationMailer, type: :mailer do
  it 'loads' do
    # ApplicationMailer is a Rails-generated class that we haven't
    # modified, so there is nothing to test.
    # This spec is only here to increase Simplecov coverage.
  end
end
